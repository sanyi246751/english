{
  "name": "english-ocr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "push": "git add . && git commit -m \"auto push\" && git push",
    "deploy": "npm run build && npx -y gh-pages -d dist",
    "preview": "vite preview",
    "lint": "npx -y prettier --check ."
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
